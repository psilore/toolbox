---
name: On Push non main branches

run-name: '🔫 Pushing changes to ${{ github.ref }} by ${{ github.actor }}'

on:
  push:
    branches-ignore:
      - main

permissions:
  contents: read
  pull-requests: read
  checks: write

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
