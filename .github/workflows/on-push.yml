---
name: 🪄 On Push main branch

run-name: 'Pushing changes to ${{ github.ref }}'

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read
  checks: write

jobs:
  tests:
    name: Run tests
    uses: ./.github/workflows/tests.yml
